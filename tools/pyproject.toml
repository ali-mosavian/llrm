[project]
name = "llrm-tools"
version = "0.1.0"
description = "Developer tools for llrm: the DOS differential harness and map/image analysis"
requires-python = ">=3.13"
dependencies = ["iced-x86>=1.21.0"]

[tool.uv]
package = false
