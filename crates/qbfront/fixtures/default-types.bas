defint a-c
deflng l-l
defsng s-s
defdbl d-d
defstr t-t

dim apple, berry, count
dim longValue, singleValue, doubleValue, textValue
print len(apple); len(berry); len(count)
print len(longValue); len(singleValue); len(doubleValue); len(textValue)
