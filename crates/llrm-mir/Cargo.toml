[package]
name = "llrm-mir"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "llrm's rich portable MIR: types, SSA values, verifier, text form, interpreter"

# No dependencies: MIR names no decoder, object file or target, and the
# dependency graph is what enforces it (see `tests::the_crate_depends_on_nothing`).
[dependencies]
