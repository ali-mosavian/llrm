fn bump(values: &mut [i16]) -> void:
    values[1] += 3
fn main() -> i16:
    let mut values: i16[3] = [10, 20, 30]
    bump(&mut values)
    if values[1] == 23:
        print("ok")
    else:
        print("bad")
    return 0
