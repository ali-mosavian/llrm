[package]
name = "zed_nib"
version = "0.1.5"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
zed_extension_api = "0.7.0"

# Built by Zed to wasm, apart from the compiler's workspace.
[workspace]
