' This source is intentionally compiled with /G3 by the negative probe.
' PDS 7.10 accepts the source but reports /G3 as an ignored unknown option.

print "REFERENCE ONLY pds-switch-g3-rejected"
end
