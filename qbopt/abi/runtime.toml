# What each QuickBASIC 4.5 runtime routine does, one row per routine.
#
# Data, not code: every field here is a claim about the runtime, and a
# claim wants an audit trail more than it wants a Python literal.
# `evidence` says where each came from -- the runtime source for the
# register and control facts, and `tools/runtime_writes.py` reading a
# linked image for what it writes.
#
# `established = false` means nothing here is known: the row concedes
# every register and every byte of memory, which is the only safe answer
# to a routine nobody has read.
#
# `cleanup = -1` is "not known", which is not the same as zero.

["B$HARY"]
cleanup = -1
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "ANY"
clobbers = ["bx", "es", "flags"]
established = true
inputs = ["bx"]
evidence = "Shipped dynamic.asm 1:00dc (QB) / hugearr.asm 1:0004 (PDS/VBDOS): BX descriptor and stack indices/rank; returns ES:BX. Entry pushes BP,AX,CX,DX,SI,DI and the normal exit restores them. XOR SI,SI kills incoming flags. Bounds/rank/unallocated failures enter ERR_BS. CleanStack removes 2*(rank+1) bytes through runtime scratch; cleanup is deliberately unknown until the call site's rank is established. Descriptor reads and error handling are not pure."

["B$LINA"]
cleanup = 0
control = "unknown"
enters_user_code = true
raises_error = true
error_handling = false
writes = "ANY"
reads = "ANY"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = true
inputs = ["ax", "bx", "cx", "di", "dx", "si"]
evidence = "Shipped dbtrace.asm 1:0000..002b in BCOM45/BCL71ENR/VBDCL10E: both ordinary exits POP BP; RETF without stack arguments. Bound GP inputs conservatively by all six registers. The /D keyboard poll installed by DBINI clears arithmetic flags before dispatch; LINA tests its own trace flag after BREAK_CHK. Trace output, break handling and indirect dependencies retain worst-case effects."


["B$?EVT"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = false
error_handling = false
writes = "OWN"
reads = "NONE"
clobbers = ["ax", "bx", "cx", "ds", "dx", "flags"]
established = true
inputs = []
evidence = '''
rt/gwaevt.asm: `cProc B$?EVT,<FAR,PUBLIC>,<ES,SI,DI>`, no parameters. It clears b$TRPTBL to flag-zero/address--1 entries, zeroes b$TRAP_SEM, inits the b$TRAP_QUE descriptor through B$INITQ and calls [b$pInitKeys1] and [b$pInitKeys2] -- all runtime state, no user variable in sight, and it installs handlers rather than running one. Memory stays the worst case because those three callees were not followed. Emitted only under /V or /W, which the optimiser refuses anyway.
'''

["B$CEND"]
cleanup = 0
control = "never"
enters_user_code = false
raises_error = false
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = true
inputs = []
evidence = '''
rt/rtterm.asm: `cProc B$CEND,<PUBLIC,FAR,FORCEFRAME>`, no parameters, `Exceptions: Does not return.` -- END and SYSTEM. It zeroes b$errnum and branches to the END executor, closing files and terminating the runtime.
'''

["B$CENP"]
cleanup = 0
control = "never"
enters_user_code = true
raises_error = false
error_handling = false
writes = "ANY"
reads = "ANY"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = true
inputs = []
evidence = '''
rt/rtterm.asm: `cProc B$CENP,<PUBLIC,FAR,FORCEFRAME>`, no parameters, `Exceptions: Does not return.` -- the default end procedure, and the last call in any BASIC module. With an ON ERROR in progress it is `JMP FAR PTR B$ERR_NR`, handing a No RESUME error to the user's handler; otherwise it falls into termination. extent.py measured what follows one: in divmod-v-g3.obj the /X RESUME map begins exactly where its `call far B$CENP` ends, so the bytes after the call are data.
'''

["B$CPI4"]
cleanup = 8
control = "returns"
enters_user_code = false
raises_error = false
error_handling = false
writes = "NONE"
reads = "ARGUMENTS"
clobbers = ["flags"]
established = true
documented = ["ax", "bx", "cx", "dx"]
inputs = []
evidence = '''
rt/helpi4.asm: `cProc B$CPI4,<FAR,PUBLIC>,<AX>` with parmD op1, parmD op2. The body reads both arguments off the frame and ends with lahf/and/shr/shl/or/sahf -- it names cx, dx and bx nowhere, and the <AX> save list restores ax, where B$MUI4/B$DVI4/B$RMI4 declare an empty one. Its own "Uses: ax,cx,dx,bx" overstates that, and its "Exceptions: hardware divide overflow" is copied from the divide above it: there is no division in the routine. Flags are the return value.
'''

["B$DDIM"]
cleanup = -1
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = true
inputs = []
evidence = '''
rt/dynamic.asm: `B$DDIM - DIM a dynamic array`, `void pascal B$DDIM(I2 lo1, I2 hi1, ..., I2 loN, I2 hiN, I2 cbelem, U2 ndims+typ<<8, ad *pAd)`. Cleanup is not a constant: the header says `Input parameters are removed from the stack` and the count is two words per dimension plus three, so None rather than a number. It falls into DIM_COMMON, which erases any present array and allocates -- writing the descriptor at pAd and moving the heap, so Memory.ANY on both sides. `Modifies: Per convention` names no preserved register, so every one is taken as clobbered.
'''

["B$DSEG"]
cleanup = 2
control = "returns"
enters_user_code = false
raises_error = false
error_handling = false
writes = "NONE"
reads = "ARGUMENTS"
clobbers = ["ax"]
established = true
documented = []
inputs = []
evidence = '''
rt/rtinit.asm: `cProc B$DSEG,<PUBLIC,FAR>` with parmW newseg, whole body `MOV AX,newseg` / `MOV [b$seg],AX`, header `Modifies: NONE` -- which is wrong by one register. With the cmacros prologue and epilogue that is push bp / mov bp,sp / two movs / mov sp,bp / pop bp / ret 2, and not one of them writes a flag. b$seg is the runtime's own DGROUP word that PEEK and POKE read later, not anything the caller can name.
'''

["B$DSG0"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = false
error_handling = false
writes = "NONE"
reads = "NONE"
clobbers = []
established = true
inputs = []
evidence = '''
rt/rtinit.asm, CODE:299, five bytes whole: `mov [b$seg],ds` / `retf`, the store's fixup segment 4 + 197 -- BR_DATA:197, the same b$seg word B$DSEG writes. Bare DEF SEG, restoring the default segment. It touches no register and no flag, so clobbers is empty rather than B$DSEG's ax. writes is NONE on B$DSEG's argument, not a second measurement.
'''

["B$DVI4"]
cleanup = 8
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "NONE"
reads = "ARGUMENTS"
clobbers = ["ax", "bx", "cx", "dx", "flags"]
established = true
inputs = []
evidence = '''
rt/helpi4.asm: `jmp __aFldiv`, `Uses: ax,cx,dx,bx`, `Exceptions: hardware divide overflow`. __aFldiv is not in the local tree. AGENTS.md measured what the header calls a hardware trap: `x \ 0` raises BASIC error 11, and -2147483648 \ -1 raises nothing at all and returns. calls.py absorbs this call as a bare idiv and drops error 11 deliberately, which is the one place a rewritten program behaves worse than BC's.
'''

["B$ENRA"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = true
evidence = '''
inc/stack.inc: `The basic frame is set up on entry to the main program, by B$ENSA/B$ENRA SUB and FUNCTION entry routines`, and it names what lands there -- the previous BASIC frame at FR_BFRAME, si and di at FR_SI/FR_DI `Preserved for C compatability`, the local byte count at FR_CLOCALS and a zeroed GOSUB count at FR_GOSUB. inc/rtmint.inc and inc/ulib.inc list it as an entry family with B$ENSA/B$ENRD/B$ENSD/B$ENFA; the code is in none of them. AGENTS.md measured the call site: the frame size arrives in cx, so nothing is on the stack to clean up. It moves both sp and bp, which is why it is in clobbers and why nothing may track stack depth across it.
'''

["B$ERAS"]
cleanup = 2
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bx", "cx", "dx", "es", "flags"]
documented = ["ax", "bx", "cx", "dx"]
established = true
inputs = []
evidence = '''
rt/dynamic.asm: `B$ERAS - erase an array`, `Entry: pAd ds relative array descriptor offset`, `Uses: AX, BX, CX, DX`. It is `MOV AX,1` then `SKIP 2` (`DB 03DH`, swallowing B$IErase's `XOR AX,AX`) into `cProc ERASE_COMMON,FAR,<SI,DI>` with one `parmW pAd`, so RETF 2 and si/di/bp restored. It clears the descriptor at pAd and frees the storage: B$LHDALC and B$LH_CPCT move the local heap, B$FHDealloc unlinks a far heap descriptor, and a static array is cleared through `GETSEG ES` and `REP STOSW` -- so es is clobbered beyond the documented four. `Exceptions: None`, but B$LH_CPCT raises the nontrappable B$ERR_SSC on heap corruption.
'''

["B$EVCK"]
cleanup = 0
control = "unknown"
enters_user_code = true
raises_error = false
error_handling = false
writes = "ANY"
reads = "ANY"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = true
inputs = []
evidence = '''
rt/gwaevt.asm: `cProc B$EVCK,<PUBLIC,FAR>`, no parameters, `Exit: GOSUBs to event handler, if so indicated`. On a pending trap it dequeues the request, calls B$FRAMESETUP, bumps FR_GOSUB and ends `jmp dword ptr[BX+1]` into the user's ON TIMER/ON KEY handler (or B$IEvHandler, which `DOESN'T RETURN`). So it can assign any module-level variable the handler names, and control leaves through the event-GOSUB machinery rather than off its own RET.
'''

["B$EXSA"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = false
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = true
evidence = '''
inc/stack.inc: si and di are `restored by B$EXSA`, and [b$curframe] is saved at FR_BFRAME `such that B$EXSA can restore it on exit`. inc/ulib.inc pairs it with B$EXFA as the exit family and rt/error.asm calls it to `clear frame state info`. The code is not in the tree. AGENTS.md measured the call site: a far call with no arguments, immediately before the procedure's own `retf n`. Like B$ENRA it moves sp and bp.
'''

["B$FCMD"]
cleanup = -1
control = "unknown"
enters_user_code = true
raises_error = true
error_handling = false
writes = "ANY"
reads = "ANY"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = false
evidence = '''
rt/oscmd.asm has `cProc B$FCMD,<PUBLIC,FAR>` / `cBegin <nogen>` / `jmp B$FrameAFE`, and B$FrameAFE is nowhere in the tree. Its header claims `Modifies: NONE` and `AX = ptr to string desc for temp with command line`, which means it allocates and so can reach B$STCPCT, but a header over an unreadable body establishes nothing. Worst case, and marked as such.
'''

["B$FCMP"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = false
error_handling = false
writes = "NONE"
reads = "NONE"
clobbers = ["ax", "flags"]
established = true
inputs = []
evidence = '''
87bhelp.asm, disassembled from all three libraries and identical in each: push bp / mov bp,sp / wait / fcompp / wait / fnstsw [0] / nop / wait / mov ah,[0] / sahf / mov sp,bp / pop bp / retf. It names bx, cx, dx, si and di nowhere; ah is the only register written, and the flags ARE the return value. sahf writes SF, ZF, AF, PF and CF and cannot write OF -- it is bit 11, outside the byte -- so the comparison arrives in CF and ZF and only the unsigned branches read it, which is what wide.COMPARISONS records and what BC emits at every site. Microsoft's own runtime agrees: rt/grwindow.asm calls it and branches with JZ and JC. The fnstsw writes one word of DGROUP, the runtime's own, which is why writes is NONE the way B$DSEG's is.
'''

["B$SIN4"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "NONE"
reads = "NONE"
clobbers = ["ax", "bx", "cx", "dx", "si", "di", "bp", "sp", "ds", "es", "flags"]
established = true
inputs = ["ax", "bx", "cx", "dx", "si", "di"]
evidence = '''
87btrig.asm, disassembled from the linked image because the math library is not in the source drop. tools/libdump.py shows one module defining B$SIN4, B$SIN8, B$COS4, B$COS8, B$TAN4, B$TAN8 and the __FI aliases, and in the library its first bytes are four `mov dx,<relocated>` stubs that all jump to one shared kernel -- six names, four stubs, because SIN4/SIN8 and COS4/COS8 each share an entry. In qbdemo's BASE.EXE that stub is at image 0x8b43 with the selectors relocated to 0x4a2, 0x4ba, 0x4d2 and 0x4ea, 24 bytes apart, and the kernel it jumps to is 0x8b81: push bp / mov bp,sp / sub sp,12h / call 0x8c09 / mov al,[bp-11h] / or al,al, and the normal path is mov sp,bp / pop bp / retf. The arithmetic at 0x8c09 is `int 35h` with the emulator's inline operand, which declen.py decodes. So: an 18-byte frame of its own below sp and the emulator's own DGROUP word, and no caller cell -- the same basis on which 87bhelp.asm's six helpers beside it in this library are NONE, and the same as B$FCMP's fnstsw. `retf` with no immediate is the cleanup: nothing arrives on the 8086 stack, the operand is on the x87 stack. One path leaves instead of returning -- `jmp 01B5h:4610h` at 0x8bd0, reached after testing al and the flag byte at [bp-6] -- so raises_error stays true and reachable() gates it on whether the program handles errors. Registers stay at the worst case: the stub itself writes dx and the kernel writes ax and bx, and nothing here establishes what the emulator tail preserves.
'''

["B$SIN8"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "NONE"
reads = "NONE"
clobbers = ["ax", "bx", "cx", "dx", "si", "di", "bp", "sp", "ds", "es", "flags"]
established = true
inputs = ["ax", "bx", "cx", "dx", "si", "di"]
evidence = '''
87btrig.asm, disassembled from the linked image because the math library is not in the source drop. tools/libdump.py shows one module defining B$SIN4, B$SIN8, B$COS4, B$COS8, B$TAN4, B$TAN8 and the __FI aliases, and in the library its first bytes are four `mov dx,<relocated>` stubs that all jump to one shared kernel -- six names, four stubs, because SIN4/SIN8 and COS4/COS8 each share an entry. In qbdemo's BASE.EXE that stub is at image 0x8b43 with the selectors relocated to 0x4a2, 0x4ba, 0x4d2 and 0x4ea, 24 bytes apart, and the kernel it jumps to is 0x8b81: push bp / mov bp,sp / sub sp,12h / call 0x8c09 / mov al,[bp-11h] / or al,al, and the normal path is mov sp,bp / pop bp / retf. The arithmetic at 0x8c09 is `int 35h` with the emulator's inline operand, which declen.py decodes. So: an 18-byte frame of its own below sp and the emulator's own DGROUP word, and no caller cell -- the same basis on which 87bhelp.asm's six helpers beside it in this library are NONE, and the same as B$FCMP's fnstsw. `retf` with no immediate is the cleanup: nothing arrives on the 8086 stack, the operand is on the x87 stack. One path leaves instead of returning -- `jmp 01B5h:4610h` at 0x8bd0, reached after testing al and the flag byte at [bp-6] -- so raises_error stays true and reachable() gates it on whether the program handles errors. Registers stay at the worst case: the stub itself writes dx and the kernel writes ax and bx, and nothing here establishes what the emulator tail preserves.
'''

["B$COS4"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "NONE"
reads = "NONE"
clobbers = ["ax", "bx", "cx", "dx", "si", "di", "bp", "sp", "ds", "es", "flags"]
established = true
inputs = ["ax", "bx", "cx", "dx", "si", "di"]
evidence = '''
87btrig.asm, disassembled from the linked image because the math library is not in the source drop. tools/libdump.py shows one module defining B$SIN4, B$SIN8, B$COS4, B$COS8, B$TAN4, B$TAN8 and the __FI aliases, and in the library its first bytes are four `mov dx,<relocated>` stubs that all jump to one shared kernel -- six names, four stubs, because SIN4/SIN8 and COS4/COS8 each share an entry. In qbdemo's BASE.EXE that stub is at image 0x8b43 with the selectors relocated to 0x4a2, 0x4ba, 0x4d2 and 0x4ea, 24 bytes apart, and the kernel it jumps to is 0x8b81: push bp / mov bp,sp / sub sp,12h / call 0x8c09 / mov al,[bp-11h] / or al,al, and the normal path is mov sp,bp / pop bp / retf. The arithmetic at 0x8c09 is `int 35h` with the emulator's inline operand, which declen.py decodes. So: an 18-byte frame of its own below sp and the emulator's own DGROUP word, and no caller cell -- the same basis on which 87bhelp.asm's six helpers beside it in this library are NONE, and the same as B$FCMP's fnstsw. `retf` with no immediate is the cleanup: nothing arrives on the 8086 stack, the operand is on the x87 stack. One path leaves instead of returning -- `jmp 01B5h:4610h` at 0x8bd0, reached after testing al and the flag byte at [bp-6] -- so raises_error stays true and reachable() gates it on whether the program handles errors. Registers stay at the worst case: the stub itself writes dx and the kernel writes ax and bx, and nothing here establishes what the emulator tail preserves.
'''

["B$COS8"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "NONE"
reads = "NONE"
clobbers = ["ax", "bx", "cx", "dx", "si", "di", "bp", "sp", "ds", "es", "flags"]
established = true
inputs = ["ax", "bx", "cx", "dx", "si", "di"]
evidence = '''
87btrig.asm, disassembled from the linked image because the math library is not in the source drop. tools/libdump.py shows one module defining B$SIN4, B$SIN8, B$COS4, B$COS8, B$TAN4, B$TAN8 and the __FI aliases, and in the library its first bytes are four `mov dx,<relocated>` stubs that all jump to one shared kernel -- six names, four stubs, because SIN4/SIN8 and COS4/COS8 each share an entry. In qbdemo's BASE.EXE that stub is at image 0x8b43 with the selectors relocated to 0x4a2, 0x4ba, 0x4d2 and 0x4ea, 24 bytes apart, and the kernel it jumps to is 0x8b81: push bp / mov bp,sp / sub sp,12h / call 0x8c09 / mov al,[bp-11h] / or al,al, and the normal path is mov sp,bp / pop bp / retf. The arithmetic at 0x8c09 is `int 35h` with the emulator's inline operand, which declen.py decodes. So: an 18-byte frame of its own below sp and the emulator's own DGROUP word, and no caller cell -- the same basis on which 87bhelp.asm's six helpers beside it in this library are NONE, and the same as B$FCMP's fnstsw. `retf` with no immediate is the cleanup: nothing arrives on the 8086 stack, the operand is on the x87 stack. One path leaves instead of returning -- `jmp 01B5h:4610h` at 0x8bd0, reached after testing al and the flag byte at [bp-6] -- so raises_error stays true and reachable() gates it on whether the program handles errors. Registers stay at the worst case: the stub itself writes dx and the kernel writes ax and bx, and nothing here establishes what the emulator tail preserves.
'''

["B$TAN4"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "NONE"
reads = "NONE"
clobbers = ["ax", "bx", "cx", "dx", "si", "di", "bp", "sp", "ds", "es", "flags"]
established = true
inputs = ["ax", "bx", "cx", "dx", "si", "di"]
evidence = '''
87btrig.asm, disassembled from the linked image because the math library is not in the source drop. tools/libdump.py shows one module defining B$SIN4, B$SIN8, B$COS4, B$COS8, B$TAN4, B$TAN8 and the __FI aliases, and in the library its first bytes are four `mov dx,<relocated>` stubs that all jump to one shared kernel -- six names, four stubs, because SIN4/SIN8 and COS4/COS8 each share an entry. In qbdemo's BASE.EXE that stub is at image 0x8b43 with the selectors relocated to 0x4a2, 0x4ba, 0x4d2 and 0x4ea, 24 bytes apart, and the kernel it jumps to is 0x8b81: push bp / mov bp,sp / sub sp,12h / call 0x8c09 / mov al,[bp-11h] / or al,al, and the normal path is mov sp,bp / pop bp / retf. The arithmetic at 0x8c09 is `int 35h` with the emulator's inline operand, which declen.py decodes. So: an 18-byte frame of its own below sp and the emulator's own DGROUP word, and no caller cell -- the same basis on which 87bhelp.asm's six helpers beside it in this library are NONE, and the same as B$FCMP's fnstsw. `retf` with no immediate is the cleanup: nothing arrives on the 8086 stack, the operand is on the x87 stack. One path leaves instead of returning -- `jmp 01B5h:4610h` at 0x8bd0, reached after testing al and the flag byte at [bp-6] -- so raises_error stays true and reachable() gates it on whether the program handles errors. Registers stay at the worst case: the stub itself writes dx and the kernel writes ax and bx, and nothing here establishes what the emulator tail preserves.
'''

["B$TAN8"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "NONE"
reads = "NONE"
clobbers = ["ax", "bx", "cx", "dx", "si", "di", "bp", "sp", "ds", "es", "flags"]
established = true
inputs = ["ax", "bx", "cx", "dx", "si", "di"]
evidence = '''
87btrig.asm, disassembled from the linked image because the math library is not in the source drop. tools/libdump.py shows one module defining B$SIN4, B$SIN8, B$COS4, B$COS8, B$TAN4, B$TAN8 and the __FI aliases, and in the library its first bytes are four `mov dx,<relocated>` stubs that all jump to one shared kernel -- six names, four stubs, because SIN4/SIN8 and COS4/COS8 each share an entry. In qbdemo's BASE.EXE that stub is at image 0x8b43 with the selectors relocated to 0x4a2, 0x4ba, 0x4d2 and 0x4ea, 24 bytes apart, and the kernel it jumps to is 0x8b81: push bp / mov bp,sp / sub sp,12h / call 0x8c09 / mov al,[bp-11h] / or al,al, and the normal path is mov sp,bp / pop bp / retf. The arithmetic at 0x8c09 is `int 35h` with the emulator's inline operand, which declen.py decodes. So: an 18-byte frame of its own below sp and the emulator's own DGROUP word, and no caller cell -- the same basis on which 87bhelp.asm's six helpers beside it in this library are NONE, and the same as B$FCMP's fnstsw. `retf` with no immediate is the cleanup: nothing arrives on the 8086 stack, the operand is on the x87 stack. One path leaves instead of returning -- `jmp 01B5h:4610h` at 0x8bd0, reached after testing al and the flag byte at [bp-6] -- so raises_error stays true and reachable() gates it on whether the program handles errors. Registers stay at the worst case: the stub itself writes dx and the kernel writes ax and bx, and nothing here establishes what the emulator tail preserves.
'''

["B$FERR"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = false
error_handling = true
writes = "NONE"
reads = "NONE"
clobbers = ["ax"]
established = true
inputs = []
evidence = '''
rt/error.asm: `cProc B$FERR,<FAR,PUBLIC>` with no parameters and the single instruction `MOV AX,[b$errnum]` -- the ERR function, and nothing more. It cannot itself reach a handler, contrary to the rest of its file; what it does prove is that the module has ON ERROR, which is what error_handling records and why barrier() still refuses a body containing it.
'''

["B$FIL2"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = false
error_handling = false
writes = "NONE"
reads = "NONE"
clobbers = ["dx", "flags"]
established = true
inputs = ["ax"]
evidence = '''
87bhelp.asm: one instruction before B$FILD's own entry -- `cwd`, then it falls straight through. So it is the INTEGER form, taking ax alone and sign-extending it, and the cwd is exactly why dx is clobbered here and preserved there.
'''

["B$FILD"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = false
error_handling = false
writes = "NONE"
reads = "NONE"
clobbers = ["flags"]
established = true
inputs = ["ax", "dx"]
evidence = '''
87bhelp.asm: push bp / mov bp,sp / push bx / push dx / push ax / mov bx,sp / wait / fild dword [bx] / add sp,4 / pop bx / mov sp,bp / pop bp / retf. It takes a LONG in dx:ax and pushes it to the x87 stack through four bytes of its own frame. dx and ax are read and never written, bx is saved and restored, and nothing else is named -- so the flags `add sp,4` leaves are the only thing that changes.
'''

["B$FIS2"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = false
error_handling = false
writes = "NONE"
reads = "NONE"
clobbers = ["ax", "flags"]
established = true
inputs = []
evidence = '''
87bhelp.asm: the INTEGER form of B$FIST -- sub sp,2 / fistp word [bp-2] / pop ax. One word, so ax alone comes back changed and dx does not.
'''

["B$FIST"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = false
error_handling = false
writes = "NONE"
reads = "NONE"
clobbers = ["ax", "dx", "flags"]
established = true
inputs = []
evidence = '''
87bhelp.asm: push bp / mov bp,sp / sub sp,4 / wait / fistp dword [bp-4] / nop / wait / pop ax / pop dx / mov sp,bp / pop bp / retf. The LONG it produces comes back in dx:ax, which is what clobbers them; bx, cx, si and di are named nowhere.
'''

["B$FUST"]
cleanup = 0
control = "returns"
enters_user_code = false
raises_error = false
error_handling = false
writes = "NONE"
reads = "NONE"
clobbers = ["ax", "flags"]
established = true
inputs = []
evidence = '''
87bhelp.asm: stores a dword, and where the high word is not zero re-loads it and stores it back as a word -- the unsigned INTEGER conversion. It ends `pop ax / add sp,2`, so only the low word is taken and dx is untouched.
'''

["B$FVAL"]
cleanup = 2
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "STRINGS"
clobbers = ["ax", "bx", "cx", "di", "ds", "dx", "es", "flags"]
established = true
inputs = []
evidence = '''
rt/fin.asm: `cProc B$FVAL,<FAR,PUBLIC>,<SI>` with parmSD sdNum (parmW, per inc/string.inc), so si survives whatever the scanner does. It calls B$STPUTZ to zero-terminate the caller's string data in place, sets b$VTYP, runs B$FIN and returns the address of B$DAC; B$FIN is the mathpack's input scanner and floating point, not modelled here, and its errors are B$ERR_OV and B$ERR_TM. Memory is the worst case because B$FIN was not read.
'''

["B$LTRM"]
cleanup = 2
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "STRINGS"
reads = "STRINGS"
clobbers = ["ax", "bx", "cx", "dx", "flags"]
established = true
inputs = []
evidence = '''
rt/strfcn.asm: B$LTRM is `MOV AH,TR_LEFT` then a two-byte skip into `cProc TRIM,FAR,<DI,ES>` with parmW psd, `Uses: Per convention`, so di and es come back and the cleanup is TRIM's 2 bytes. It scans the caller's string data and calls B$STALCTMPSUB for the result, reaching B$STCPCT. The right-trim path sets the direction flag but every exit passes the following CLD, matching ifout.asm's `PSW.D clear`.
'''

["B$MUI4"]
cleanup = 8
control = "returns"
enters_user_code = false
raises_error = false
error_handling = false
writes = "NONE"
reads = "ARGUMENTS"
clobbers = ["ax", "bx", "cx", "dx", "flags"]
established = true
inputs = []
evidence = '''
rt/helpi4.asm: `B$MUI4(I4 op1,I4 op2)`, `Uses: ax,cx,dx,bx`, `Exceptions: none`, and a body that is `jmp __aFlmul` with no frame. __aFlmul is the C library's own long multiply and is NOT in the local tree, so the register set is the header's word rather than read code; the cleanup is the C helper's, and BC's measured call sites (calls.py, stack.py) say it happens. AGENTS.md measured the overflow: 305419896 * 252645135 wraps and raises nothing.
'''

["B$OEGA"]
cleanup = 4
control = "unknown"
enters_user_code = true
raises_error = false
error_handling = true
writes = "ANY"
reads = "ANY"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = true
inputs = []
evidence = '''
rt/error.asm: `cProc B$OEGA,<FAR,PUBLIC>` with parmD erradr -- the ON ERROR GOTO statement handler. It sets OFD_ONERROR in the module's own data, and its header says `erradr == 0 and an error is in progress, will jump to B$SERR instead of returning`: OEGA_10 does `MOV BP,[BP]` / `ADD SP,8` and `JMP B$SERR ;Process error (Never Returns)`, which is the path into the user's handler.
'''

["B$OGTA"]
cleanup = -1
control = "inline-table"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = true
inputs = ["bx"]
evidence = '''
Named by inc/ulib.inc and inc/rtmint.inc; the code is not in the tree. What is established is measured, not read: blocks.py found that ON GOTO compiles to a call to it followed by inline data -- a count byte and that many offset16 words, each a fixup into this segment -- which it reads via its own return address, and the branch index arrives in bx -- all 16 sites in the corpus are `mov bx` immediately before the call, with nothing else unanimous. Measured rather than read, for the same reason the rest of this entry is. blocks.INLINE_TABLE is where that lives and where the control kind below comes from, so the two cannot drift.
'''

["B$PEI2"]
cleanup = 2
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bx", "cx", "di", "ds", "dx", "es", "flags"]
established = true
inputs = []
evidence = '''
rt/prnval.asm: the entry point is `MOV AX,<term> SHL 8 + <type>` then `JMP SHORT B$PRINT` (B$PESD falls straight in). `cProc B$PRINT,<PUBLIC,FAR>,<SI>` saves si and the epilogue restores si and bp before returning, so those two survive; the rest does not, because the numeric path calls B$FOUTBX and the file comment there says it `changes all registers except BP` -- flatly contradicting ifout.asm's "Per convention. (DS, ES, SI, DI, BP preserved.)" for the same routine, and the conservative reading wins. PRINTX pops one parameter word, then a second unless `TEST AL,VT_SD` is non-zero, which it is for VT_I2=2 and VT_SD=3 and is not for VT_I4=14h (inc/rtps.inc). Memory is the worst case and read as such: print using goes through the [b$PUSG] vector into prtu.asm, which calls B$SASS, and output goes through the [VTYP], [VWCH] and [b$pFLUSH] vectors whose targets depend on the open device. Any string allocation on those paths reaches B$STALC, whose step 4 calls B$STCPCT -- and rt/nhstutil.asm says of it `The string descriptors referenced by the string header are adjusted to reflect their movement`, writing [BX+2] of every live descriptor including a module-level string variable's. Header: `Exceptions: bad file mode; I/O error or Disk full error when flush the buffer if a EOL encountered`.
'''

["B$PEI4"]
cleanup = 4
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bx", "cx", "di", "ds", "dx", "es", "flags"]
established = true
inputs = []
evidence = '''
rt/prnval.asm: the entry point is `MOV AX,<term> SHL 8 + <type>` then `JMP SHORT B$PRINT` (B$PESD falls straight in). `cProc B$PRINT,<PUBLIC,FAR>,<SI>` saves si and the epilogue restores si and bp before returning, so those two survive; the rest does not, because the numeric path calls B$FOUTBX and the file comment there says it `changes all registers except BP` -- flatly contradicting ifout.asm's "Per convention. (DS, ES, SI, DI, BP preserved.)" for the same routine, and the conservative reading wins. PRINTX pops one parameter word, then a second unless `TEST AL,VT_SD` is non-zero, which it is for VT_I2=2 and VT_SD=3 and is not for VT_I4=14h (inc/rtps.inc). Memory is the worst case and read as such: print using goes through the [b$PUSG] vector into prtu.asm, which calls B$SASS, and output goes through the [VTYP], [VWCH] and [b$pFLUSH] vectors whose targets depend on the open device. Any string allocation on those paths reaches B$STALC, whose step 4 calls B$STCPCT -- and rt/nhstutil.asm says of it `The string descriptors referenced by the string header are adjusted to reflect their movement`, writing [BX+2] of every live descriptor including a module-level string variable's. Header: `Exceptions: bad file mode; I/O error or Disk full error when flush the buffer if a EOL encountered`.
'''

["B$PER4"]
cleanup = 4
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bx", "cx", "di", "ds", "dx", "es", "flags"]
established = true
inputs = []
evidence = '''
rt/prnval.asm: the entry point is `MOV AX,<term> SHL 8 + <type>` then `JMP SHORT B$PRINT` (B$PESD falls straight in). `cProc B$PRINT,<PUBLIC,FAR>,<SI>` saves si and the epilogue restores si and bp before returning, so those two survive; the rest does not, because the numeric path calls B$FOUTBX and the file comment there says it `changes all registers except BP` -- flatly contradicting ifout.asm's "Per convention. (DS, ES, SI, DI, BP preserved.)" for the same routine, and the conservative reading wins. PRINTX pops one parameter word, then a second unless `TEST AL,VT_SD` is non-zero, which it is for VT_I2=2 and VT_SD=3 and is not for VT_I4=14h (inc/rtps.inc). Memory is the worst case and read as such: print using goes through the [b$PUSG] vector into prtu.asm, which calls B$SASS, and output goes through the [VTYP], [VWCH] and [b$pFLUSH] vectors whose targets depend on the open device. Any string allocation on those paths reaches B$STALC, whose step 4 calls B$STCPCT -- and rt/nhstutil.asm says of it `The string descriptors referenced by the string header are adjusted to reflect their movement`, writing [BX+2] of every live descriptor including a module-level string variable's. Header: `Exceptions: bad file mode; I/O error or Disk full error when flush the buffer if a EOL encountered`.
'''

["B$PER8"]
cleanup = 8
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bx", "cx", "di", "ds", "dx", "es", "flags"]
established = true
inputs = []
evidence = '''
rt/prnvalfp.asm: `MOV AX,EOL SHL 8 + VT_R8` then `JMP B$PRINT`, the same entry as B$PER4 with VT_R8=08h (inc/rtps.inc); BCOM45.LIB 0028, BCL71ENR.LIB and VBDCL10E.LIB 001e all read `mov ax,208h` / `jmp`. Everything else is B$PER4's B$PRINT: si and bp survive, the rest does not, and memory is the worst case. PRINTX pops one parameter word, a second because `TEST AL,VT_SD` is zero for 08h, and a third and fourth because `TEST AL,8` is not; BCOM45.LIB B$PRINT 00e2..00f3 matches and re-pushes the far return address before RETF, so the cleanup is 8.
'''

["B$PESD"]
cleanup = 2
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bx", "cx", "di", "ds", "dx", "es", "flags"]
established = true
inputs = []
evidence = '''
rt/prnval.asm: the entry point is `MOV AX,<term> SHL 8 + <type>` then `JMP SHORT B$PRINT` (B$PESD falls straight in). `cProc B$PRINT,<PUBLIC,FAR>,<SI>` saves si and the epilogue restores si and bp before returning, so those two survive; the rest does not, because the numeric path calls B$FOUTBX and the file comment there says it `changes all registers except BP` -- flatly contradicting ifout.asm's "Per convention. (DS, ES, SI, DI, BP preserved.)" for the same routine, and the conservative reading wins. PRINTX pops one parameter word, then a second unless `TEST AL,VT_SD` is non-zero, which it is for VT_I2=2 and VT_SD=3 and is not for VT_I4=14h (inc/rtps.inc). Memory is the worst case and read as such: print using goes through the [b$PUSG] vector into prtu.asm, which calls B$SASS, and output goes through the [VTYP], [VWCH] and [b$pFLUSH] vectors whose targets depend on the open device. Any string allocation on those paths reaches B$STALC, whose step 4 calls B$STCPCT -- and rt/nhstutil.asm says of it `The string descriptors referenced by the string header are adjusted to reflect their movement`, writing [BX+2] of every live descriptor including a module-level string variable's. Header: `Exceptions: bad file mode; I/O error or Disk full error when flush the buffer if a EOL encountered`.
'''

["B$PSI4"]
cleanup = 4
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bx", "cx", "di", "ds", "dx", "es", "flags"]
established = true
inputs = []
evidence = '''
rt/prnval.asm: the entry point is `MOV AX,<term> SHL 8 + <type>` then `JMP SHORT B$PRINT` (B$PESD falls straight in). `cProc B$PRINT,<PUBLIC,FAR>,<SI>` saves si and the epilogue restores si and bp before returning, so those two survive; the rest does not, because the numeric path calls B$FOUTBX and the file comment there says it `changes all registers except BP` -- flatly contradicting ifout.asm's "Per convention. (DS, ES, SI, DI, BP preserved.)" for the same routine, and the conservative reading wins. PRINTX pops one parameter word, then a second unless `TEST AL,VT_SD` is non-zero, which it is for VT_I2=2 and VT_SD=3 and is not for VT_I4=14h (inc/rtps.inc), so this one pops both. Its own entry is `MOV AX,SEMI SHL 8 + VT_I4` -- `mov ax,114h` in the linked image under all three compilers, at 0x189 under VBDOS, 0x5db under PDS 7.1 and 0x1ef6 under QuickBASIC 4.5 -- and reads no register. Memory is the worst case and read as such: print using goes through the [b$PUSG] vector into prtu.asm, which calls B$SASS, and output goes through the [VTYP], [VWCH] and [b$pFLUSH] vectors whose targets depend on the open device. Any string allocation on those paths reaches B$STALC, whose step 4 calls B$STCPCT -- and rt/nhstutil.asm says of it `The string descriptors referenced by the string header are adjusted to reflect their movement`, writing [BX+2] of every live descriptor including a module-level string variable's. Header: `Exceptions: bad file mode; I/O error or Disk full error when flush the buffer if a EOL encountered`.
'''

["B$PSI2"]
cleanup = 2
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bx", "cx", "di", "ds", "dx", "es", "flags"]
established = true
inputs = []
evidence = '''
rt/prnval.asm: the entry point is `MOV AX,<term> SHL 8 + <type>` then `JMP SHORT B$PRINT` (B$PESD falls straight in). `cProc B$PRINT,<PUBLIC,FAR>,<SI>` saves si and the epilogue restores si and bp before returning, so those two survive; the rest does not, because the numeric path calls B$FOUTBX and the file comment there says it `changes all registers except BP` -- flatly contradicting ifout.asm's "Per convention. (DS, ES, SI, DI, BP preserved.)" for the same routine, and the conservative reading wins. PRINTX pops one parameter word, then a second unless `TEST AL,VT_SD` is non-zero, which it is for VT_I2=2 and VT_SD=3 and is not for VT_I4=14h (inc/rtps.inc). Memory is the worst case and read as such: print using goes through the [b$PUSG] vector into prtu.asm, which calls B$SASS, and output goes through the [VTYP], [VWCH] and [b$pFLUSH] vectors whose targets depend on the open device. Any string allocation on those paths reaches B$STALC, whose step 4 calls B$STCPCT -- and rt/nhstutil.asm says of it `The string descriptors referenced by the string header are adjusted to reflect their movement`, writing [BX+2] of every live descriptor including a module-level string variable's. Header: `Exceptions: bad file mode; I/O error or Disk full error when flush the buffer if a EOL encountered`.
'''

["B$PSSD"]
cleanup = 2
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bx", "cx", "di", "ds", "dx", "es", "flags"]
established = true
inputs = []
evidence = '''
rt/prnval.asm: the entry point is `MOV AX,<term> SHL 8 + <type>` then `JMP SHORT B$PRINT` (B$PESD falls straight in). `cProc B$PRINT,<PUBLIC,FAR>,<SI>` saves si and the epilogue restores si and bp before returning, so those two survive; the rest does not, because the numeric path calls B$FOUTBX and the file comment there says it `changes all registers except BP` -- flatly contradicting ifout.asm's "Per convention. (DS, ES, SI, DI, BP preserved.)" for the same routine, and the conservative reading wins. PRINTX pops one parameter word, then a second unless `TEST AL,VT_SD` is non-zero, which it is for VT_I2=2 and VT_SD=3 and is not for VT_I4=14h (inc/rtps.inc). Memory is the worst case and read as such: print using goes through the [b$PUSG] vector into prtu.asm, which calls B$SASS, and output goes through the [VTYP], [VWCH] and [b$pFLUSH] vectors whose targets depend on the open device. Any string allocation on those paths reaches B$STALC, whose step 4 calls B$STCPCT -- and rt/nhstutil.asm says of it `The string descriptors referenced by the string header are adjusted to reflect their movement`, writing [BX+2] of every live descriptor including a module-level string variable's. Header: `Exceptions: bad file mode; I/O error or Disk full error when flush the buffer if a EOL encountered`.
'''

["B$RDI2"]
cleanup = 4
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = true
inputs = []
evidence = '''
rt/read.asm, the header above B$RDI2: `pDest = far pointer to the destination for the data`, `cbDest = for SD and FS only`; `B$RD<type> only sets the type, [b$VTYP], and then jump to a common routine, CommRead`. CommRead reaches B$ReadVal through the [b$GetOneVal] vector, so what it writes is not bounded by pDest. `Uses: per convention` names no preserved register. Exceptions: out of DATA, syntax error, overflow.
'''

["B$RDI4"]
cleanup = 4
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = true
inputs = []
evidence = '''
rt/read.asm, the header above B$RDI2: `pDest = far pointer to the destination for the data`, `cbDest = for SD and FS only`; `B$RD<type> only sets the type, [b$VTYP], and then jump to a common routine, CommRead`. CommRead reaches B$ReadVal through the [b$GetOneVal] vector, so what it writes is not bounded by pDest. `Uses: per convention` names no preserved register. Exceptions: out of DATA, syntax error, overflow.
'''

["B$RDR4"]
cleanup = 4
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = true
inputs = []
evidence = '''
rt/read.asm, the header above B$RDI2: `pDest = far pointer to the destination for the data`, `cbDest = for SD and FS only`; `B$RD<type> only sets the type, [b$VTYP], and then jump to a common routine, CommRead`. CommRead reaches B$ReadVal through the [b$GetOneVal] vector, so what it writes is not bounded by pDest. `Uses: per convention` names no preserved register. Exceptions: out of DATA, syntax error, overflow.
'''

["B$RESN"]
cleanup = 0
control = "never"
enters_user_code = true
raises_error = false
error_handling = true
writes = "ANY"
reads = "ANY"
clobbers = ["ax", "bp", "bx", "cx", "di", "ds", "dx", "es", "flags", "si", "sp"]
established = true
inputs = []
evidence = '''
rt/error.asm: `cProc B$RESN,<FAR,PUBLIC,FORCEFRAME>,SI`, no parameters -- RESUME NEXT. It searches the module's statement-address table for the entry after b$erradr and leaves through RESRET to resume there, or `JMP B$CEND` when it runs off the bottom. It never comes back to the byte after the call. A /X construct, which the optimiser does not support.
'''

["B$RMI4"]
cleanup = 8
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "NONE"
reads = "ARGUMENTS"
clobbers = ["ax", "bx", "cx", "dx", "flags"]
established = true
inputs = []
evidence = '''
rt/helpi4.asm: `jmp __aFlrem`, otherwise identical to B$DVI4, and __aFlrem is not in the tree either.
'''

["B$ASSN"]
cleanup = 12
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "OWN"
reads = "OWN"
clobbers = ["ax", "bx", "cx", "dx", "es", "flags"]
established = true
inputs = []
evidence = '''
rt/string.asm: `cProc B$ASSN,<FAR,PUBLIC>,<DI,SI,ES>` with parmD pSrc, parmW cbSrc, parmD pDest, parmW cbDest, `Uses: per convention`. It reads its operands off the stack, and `OR AX,AX` sets the flags before any branch reads them. A fixed-length destination is written through pDest outright (`REP MOVSB` / `REP STOSB`); a descriptor destination goes through B$LDFS and B$SASS, or B$LSET for a descriptor source, which reach string allocation and so B$STCPCT. BCOM45.LIB 0050..00a1 and BCL71ENR.LIB 0028..0079 are the same code: PUSH SI/DI/ES on entry, every path joins POP ES/DI/SI/BP / RETF 0Ch. ES is listed anyway: VBDCL10E.LIB's farstr B$ASSN loads it and does not restore it, and that family's own variant stays in force.
'''

["B$LDFS"]
cleanup = 6
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "STRINGS"
reads = "OWN"
clobbers = ["ax", "bx", "cx", "dx", "es", "flags"]
established = true
inputs = []
evidence = '''
rt/string.asm: `cProc B$LDFS,<PUBLIC,FAR>,<ES,SI,DI>` with parmD pString, parmW cbString; it reads [cbString] into BX, calls B$STALCTMP for a temporary (BX = descriptor, DX = data), copies from pString with DS saved around the block move, and returns the descriptor in AX. What it writes is the temporary, and allocation reaches B$STCPCT, which moves other strings: string space, not the caller's data. `Exceptions: Runtime error can occur (such as 'insufficient string space')`. BCOM45.LIB 002b..004d and BCL71ENR.LIB 0000..0025 both PUSH SI/DI/ES, POP ES/DI/SI/BP and RETF 6.
'''

["B$SASS"]
cleanup = 4
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "STRINGS"
reads = "STRINGS"
clobbers = ["ax", "bx", "cx", "dx", "flags"]
established = true
inputs = []
evidence = '''
rt/stcore.asm: `cProc B$SASS,<FAR,PUBLIC>` with parmW psdSource, parmW psdDst, `Registers: per convention`. It writes the caller's destination descriptor outright -- `MOV [BX],CX` and `MOV [BX+2],AX` with BX=psdDst -- plus the backpointer at [BX-2] in string space, and the NOTEMP path calls B$STALCTMPSUB, which reaches B$STALC and so B$STCPCT (rt/nhstutil.asm), moving every other live string and rewriting its descriptor. B$STALC's failure path is `JMP B$ERR_OS`.
'''

["B$SCAT"]
cleanup = 4
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "STRINGS"
reads = "STRINGS"
clobbers = ["ax", "bx", "cx", "dx", "flags"]
established = true
inputs = []
evidence = '''
rt/stcore.asm: `cProc B$SCAT,<FAR,PUBLIC>,<ES,DI,SI>` with parmW psd1, parmW psd2, so es, di and si come back despite the routine setting ES=DS internally. It calls B$STALCTMP for the result, which reaches B$STCPCT and rewrites live descriptors (rt/nhstutil.asm), and MOVSTR ends `JMP B$STDALCTMP` on each source, freeing it if it was a temp. Length overflow is `JO ERRFC` -> `JMP B$ERR_FC`.
'''

["B$STDL"]
cleanup = 2
control = "returns"
enters_user_code = false
raises_error = false
error_handling = false
writes = "STRINGS"
reads = "STRINGS"
clobbers = ["ax", "bx", "cx", "dx", "flags"]
established = true
inputs = []
evidence = '''
rt/string.asm: `cProc B$STDL,<PUBLIC,FAR>` with parmW pSd, `Uses: Per convention`, `Exceptions: none`. Its last instruction is `MOV WORD PTR [BX],0` on the descriptor it was handed -- a write straight into a caller variable. It reaches only B$STDALC, which goes to B$STADJ and never calls B$STCPCT (rt/nhstutil.asm), so unlike an allocation it moves no other string.
'''

["B$STI2"]
cleanup = 2
control = "returns"
enters_user_code = false
raises_error = true
error_handling = false
writes = "STRINGS"
reads = "ARGUMENTS"
clobbers = ["ax", "bx", "cx", "di", "ds", "dx", "es", "flags", "si"]
established = true
documented = ["ax", "bx", "cx", "dx", "flags"]
inputs = []
evidence = '''
rt/string.asm: `cProc B$STI2,<PUBLIC,FAR>` with ParmW I2Arg, `Uses: Per convention`, `Exceptions: Out of memory`. It falls into B$STR_COMMON, which sets b$VTYP, calls B$FOUTBX and then B$STALCTMPCPY -> B$STALCTMP -> B$STALC -> B$STCPCT, so it can move every live string and rewrite its descriptor. The header understates the clobber set in the direction B$CPI4's overstates it: rt/prnval.asm says B$FOUTBX `changes all registers except BP`, so si, di, ds and es are not the "per convention" survivors ifout.asm claims.
'''
