fn first(text: string) -> char:
    for byte in text:
        return byte
    return '\0'
fn size(text: string) -> u16:
    return text.len + text.capacity
fn main() -> u16:
    let text: string = "abc"
    if first(text) == 'a':
        return size(text)
    return 0
